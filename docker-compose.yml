
services:
  prompt-forge:
    build:
      context: .
      args:
        - API_KEY=${API_KEY}
    container_name: prompt-forge-ai
    ports:
      - "8080:80"
    environment:
      - API_KEY=${API_KEY}
    restart: unless-stopped
